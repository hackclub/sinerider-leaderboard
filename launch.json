{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "npm run build && npm run start",
      "name": "Run npm start",
      "request": "launch",
      "type": "node-terminal"
    }
  ]
}
